include ':injectresource-annotations'
include ':injectresource-plugin'
//can be added manually if needed but can't be part of the build
//as its **buildScript** uses artifacts of previous projects.
//include ':injectresource-example'
